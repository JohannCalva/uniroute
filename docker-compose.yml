# SINCRONIZACIÓN DE RELOJES FÍSICOS (S8 — NTP):
# Todos los contenedores heredan automáticamente el reloj del host Docker,
# que se sincroniza vía NTP con servidores Stratum 2/3 del sistema operativo.
# Esto garantiza marcas de tiempo TIMESTAMPTZ consistentes en PostgreSQL
# sin necesidad de configurar NTP dentro de cada contenedor.
# Ref: https://docs.docker.com/engine/faq/#does-docker-use-the-host-clock

services:
  nginx:
    build:
      context: ./nginx
    ports:
      - "${NGINX_PORT:-80}:80"
      - "${NGINX_HTTPS_PORT:-443}:443"
    depends_on:
      - app-estudiante
      - app-conductor
      - dashboard-admin
      - api-gateway
      - websocket-server
    networks:
      - uniroute-network

  api-gateway:
    build:
      context: .
      dockerfile: packages/api-gateway/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - QR_HMAC_SECRET=${QR_HMAC_SECRET}
    depends_on:
      - usuarios-service
      - rutas-service
      - despachos-service
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - uniroute-network

  usuarios-service:
    build:
      context: .
      dockerfile: packages/usuarios-service/Dockerfile
    expose:
      - "3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - QR_HMAC_SECRET=${QR_HMAC_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - uniroute-network

  rutas-service:
    build:
      context: .
      dockerfile: packages/rutas-service/Dockerfile
    expose:
      - "3002"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3002/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - uniroute-network

  despachos-service:
    build:
      context: .
      dockerfile: packages/despachos-service/Dockerfile
    expose:
      - "3004"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - RABBITMQ_URL=${RABBITMQ_URL}
      - QR_HMAC_SECRET=${QR_HMAC_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3004/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - uniroute-network

  websocket-server:
    build:
      context: .
      dockerfile: packages/websocket-server/Dockerfile
    ports:
      - "3003:3003"
    environment:
      - RABBITMQ_URL=${RABBITMQ_URL}
    depends_on:
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3003/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - uniroute-network

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "15672:15672"  # Panel admin — remover en producción
    expose:
      - "5672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - uniroute-network

  postgres:
    image: postgres:16-alpine
    expose:
      - "5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./packages/rutas-service/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - uniroute-network

  redis:
    image: redis:7-alpine
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - uniroute-network

  app-estudiante:
    build:
      context: packages/app-estudiante
      dockerfile: Dockerfile
    expose:
      - "5173"
    networks:
      - uniroute-network

  app-conductor:
    build:
      context: packages/app-conductor
      dockerfile: Dockerfile
    expose:
      - "5174"
    networks:
      - uniroute-network

  dashboard-admin:
    build:
      context: packages/dashboard-admin
      dockerfile: Dockerfile
    expose:
      - "5175"
    networks:
      - uniroute-network

volumes:
  postgres_data:

networks:
  uniroute-network:
    driver: bridge
